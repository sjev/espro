homeassistant:
  name: Home
  unit_system: metric
  time_zone: UTC

# Includes default integrations
default_config:

# MQTT broker connection must be configured via UI:
# Settings > Devices & Services > Add Integration > MQTT
# Host: mosquitto, Port: 1883
